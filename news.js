const list=document.querySelector('#news-list');const formatDate=value=>new Intl.DateTimeFormat('de-DE',{day:'2-digit',month:'long',year:'numeric'}).format(new Date(`${value}T12:00:00`));function node(tag,className,text){const el=document.createElement(tag);if(className)el.className=className;if(text)el.textContent=text;return el}function render(post,index){const details=node('details','news-item');details.id=post.slug;if(location.hash===`#${post.slug}`||index===0)details.open=true;const summary=document.createElement('summary');const image=document.createElement('img');image.src=post.image;image.alt=post.alt;image.loading=index?'lazy':'eager';image.decoding='async';const intro=document.createElement('div');const time=node('time','',formatDate(post.date));time.dateTime=post.date;intro.append(time,node('h2','',post.titel),node('p','',post.kurztext));summary.append(image,intro,node('span','news-toggle','+'));const body=node('div','news-detail');const marker=node('p','demo-chip',post.demo?'Demo-Beitrag':'Aktuelles');const copy=document.createElement('div');for(const paragraph of post.text)copy.append(node('p','',paragraph));body.append(marker,copy);details.append(summary,body);return details}fetch('content/beitraege.json',{cache:'no-store'}).then(response=>{if(!response.ok)throw new Error();return response.json()}).then(posts=>{list.replaceChildren(...posts.sort((a,b)=>b.date.localeCompare(a.date)).map(render))}).catch(()=>{list.replaceChildren(node('p','noscript','Die Demo-Beiträge konnten nicht geladen werden.'))});
